# Configuration Sources

The repository aggregates publicly available VPN configuration sources.

Configurations are deduplicated and processed before being published.

The project does not guarantee that any third-party public server will remain available.

The current source collection is maintained by the server-side updater and is intentionally kept outside the public repository.
