# MTProto Proxy

MTProto — отдельный протокол прокси, используемый в том числе для Telegram.

Публичные MTProto-прокси могут переставать работать без предупреждения, поэтому используйте актуальные списки и проверяйте соединение непосредственно в клиенте.
